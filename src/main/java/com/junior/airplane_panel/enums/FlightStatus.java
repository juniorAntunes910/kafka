package com.junior.airplane_panel.enums;

public enum FlightStatus {
        SCHEDULED,
        CONFIRMED,
        CHECK_IN_OPEN,
        BOARDING,
        LAST_CALL,
        DELAYED,
        CANCELLED,
        DEPARTED,
        LANDED
}